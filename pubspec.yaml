name: sechserapplication
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  sensors_plus: ^4.0.2
  flutter_blue_plus: ^1.31.15
  shared_preferences: ^2.2.2
  camera: ^0.10.5+9
  flutter_local_notifications: ^14.1.3+1
  path_provider: ^2.1.2
  sqflite: ^2.3.0
  vector_math: ^2.1.4
  permission_handler: ^11.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
